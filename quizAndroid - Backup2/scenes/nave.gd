extends Node2D

# class member variables go here, for example:
var clicked = false
# var b = "textvar"

func _ready():
	# Called when the node is added to the scene for the first time.
	# Initialization here
	pass

func _process(delta):
#	clicked = get_node("Sprite/Area2D").clicked
	
	
	
#	if Input.is_action_just_pressed("Mouse left"):
#		print("mouse:", get_global_mouse_position().x)	
#		print("pos:", get_position().x)	
#		print("scale:", get_transform().get_scale().x)
#		print(clicked)

		
#		print(checkClick(get_global_mouse_position().x,get_global_mouse_position().y))
#		if(clicked):
#			if(get_transform().get_scale() == Vector2(2,2)):
#				set("scale",Vector2(1,1))
#			else:
#				set("scale",Vector2(2,2))
	pass
	
#func checkClick(mX,mY):
#	
#	print("left point:", get_position().x-get_transform().get_scale().x)
#	print("right point:", get_position().x+get_transform().get_scale().x)
#	
#	
#	if(mX>=get_position().x-(get_transform().get_scale().x*50) and mX<=get_position().x+(get_transform().get_scale().x*50)):
#		return 1
#	else:
#		return 0
#		
#	pass

#func _input(event):
#	
#   if event is InputEventMouse:
#       print("Mouse Click/Unclick at: ", event.position)
	
